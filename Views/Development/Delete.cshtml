@model TestCoWorking.Models.Booking
@{
    ViewData["Title"] = "Видалення";
    Layout = "_DevLayout";
}

<h1 class="text-center">Видалення</h1>

<div>
    <dl class="dl-horizontal">
        <dt>Ім'я</dt>
        <dd>@Model.Name</dd>

        <dt>Початок</dt>
        <dd>@Model.Start</dd>

        <dt>Кінець</dt>
        <dd>@Model.End</dd>
    </dl>

    <div>
        <form asp-controller="Development" asp-action="Delete" method="post">
            <div class="form-group">
                <input type="submit" class="form-control" value="Видалити" />
            </div>
        </form>
    </div>
</div>